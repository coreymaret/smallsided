import type { ServiceConfig } from '../types';

export const trainingConfig: ServiceConfig = {
  type: 'training',
  title: 'Training Registration',
  subtitle: 'Personalized training programs',
  component: {} as any,
};
