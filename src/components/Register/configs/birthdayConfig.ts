import type { ServiceConfig } from '../types';

export const birthdayConfig: ServiceConfig = {
  type: 'birthday',
  title: 'Birthday Party Booking',
  subtitle: 'Soccer-themed birthday parties',
  component: {} as any,
};
