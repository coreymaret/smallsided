import type { ServiceConfig } from '../types';

export const campsConfig: ServiceConfig = {
  type: 'camps',
  title: 'Camps Registration',
  subtitle: 'Soccer camps and clinics',
  component: {} as any,
};
